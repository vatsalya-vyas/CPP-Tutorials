#include <iostream>
using namespace std;

int main()
{
    // - Polymorphism poly = many, morphism = forms --> Anything that has many forms is called polymorphism (One name and multiple forms)
    // - Function overloading, operator overloading, virtual functions are example of polymorphism

    // Polymorphism in C++ can be of two types:

    // 1. Compile time polymorphism
    //    Compile time polymorphism in C++ is achieved using:
    //    1.1 Function overloading
    //    1.2 Operator overloading

    // 2. Run time polymorphism
    //    Run time polymorphism in C++ is achieved using:
    //    2.1 Virtual functions

    return 0;
}